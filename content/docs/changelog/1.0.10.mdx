---
upcoming: true
title: v1.0.10
date: September 25, 2026
---

### Systems

<ChangelogList>
    <Added>Full airport moving map with Navigraph nav data</Added>
    <Added>Runways-only airport moving map with native nav data</Added>
    <Added>`ALONG TRK WPT` function on waypoint menu</Added>
    <Added>Pilot waypoint quick-entry from FMS `ROUTE` page as `PB/D/N` or `PB/PB/N`</Added>
    <Added>Pedal disconnect button binding for `TOGGLE TAIL WHEEL LOCK` input</Added>
    <Added>Interactive rudder trim knob</Added>

    <Fixed>VNAV predictions sometimes flips headwinds and tailwinds</Fixed>
    <Fixed>Swapping frequencies does not work from the `CNS` format</Fixed>
    <Fixed>`AUTOPILOT AIRSPEED HOLD VAR` does not update after switching from Mach to IAS</Fixed>
    <Fixed>Deleting `FLT ID` on `XPDR/TCAS CONTROL` dialog should reset value to `00000000`</Fixed>
    <Fixed>`OIL TEMP` and `OIL PRESS` on EICAS rapidly change between rounded values</Fixed>
    <Fixed>Failed FMS should display `FMS NOT AVAILABLE` isntead of `FMS1/2 FAIL`</Fixed>
    <Fixed>Switching to a SEC flight plan while on `PERF DEP` or `PERF ARR` stays on disabled tab</Fixed>
    <Fixed>`TCAS OFF` should appear in white on ground, not in amber</Fixed>
    <Fixed>Extension and retraction durations for flaps and slats are not accurate</Fixed>
    <Fixed>Runway symbol on the ND should be magenta when it is the current leg</Fixed>
</ChangelogList>


#### Autoflight

<ChangelogList>
    <Fixed>Slight roll and pitch oscillations in cruise</Fixed>
    <Fixed>Slight pitch oscillations during `BARO RNP` approaches</Fixed>
    <Fixed>Jitter and oscillations at 2x and 4x simulation rate</Fixed>
    <Fixed>Ground-to-air transition commands inconsistent pitch rate</Fixed>
    <Fixed>Air-to-ground transition suddenly commands nose up upon main gear touchdown</Fixed>
    <Fixed>Capturing new FMS track is too slow, aircraft flies at a slight bank for a long time</Fixed>
    <Fixed>Excessive FD/FPV mismatch when autopilot is engaged</Fixed>
</ChangelogList>


### Art

<ChangelogList>
    <Added>Interactive gear pin stowage box</Added>

    <Fixed>Some ACP volume knobs missing decals</Fixed>
    <Fixed>Some ACP volume knobs missing interactions</Fixed>
    <Fixed>Button decals on RSP are not attached correctly when animating</Fixed>
    <Fixed>FCP buttons are hard to click from the first officer's seat</Fixed>
    <Fixed>Main landing gear pacard visible inside cabin</Fixed>
    <Fixed>`R GEN DISC` switch placed on top of the `L GEN DISC` switch</Fixed>
    <Fixed>Beacon lights are positioned outside of the light housing</Fixed>
    <Fixed>Missing interior collision mesh</Fixed>
    <Fixed>Map reading lights inoperative in MSFS 2024</Fixed>
    <Fixed>Pilot models missing in exterior view in MSFS 2024</Fixed>
    <Fixed>FCP ALT button triggers the knob event in MSFS 2020</Fixed>
</ChangelogList>
